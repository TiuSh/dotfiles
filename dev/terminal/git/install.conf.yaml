- defaults:
    link:
      create: true
      force:  true
      relink: true

- link:
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore

- brew:
  - git
  - git-extras

- shell:
  - cp ~/.dotfiles/dev/terminal/git/.gitconfig.local ~/
